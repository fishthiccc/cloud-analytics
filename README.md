## Next Tasks:
- [ ] Dockerize backend, frontend, and InfluxDB with docker-compose
- [ ] Order tables on frontend (most recent weather first, alphabetical towns)
- [ ] Set up CI/CD pipeline

## Current Setup:
- Backend: FastAPI + SQLAlchemy (SQLite for towns)
- Time Series DB: InfluxDB (weather observations)
- Frontend: React + Recharts
- Testing: pytest (backend), react-testing-library (frontend)